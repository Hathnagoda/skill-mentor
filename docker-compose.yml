services:
  backend:
    build:
      context: ./backend/backend
    container_name: skillmentor-backend
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres?sslmode=require
      SPRING_DATASOURCE_USERNAME: postgres.wcsxqwadvnorzwzpekcr
      SPRING_DATASOURCE_PASSWORD: Skillmentor@123

  frontend:
    build:
      context: ./frontend
    container_name: skillmentor-frontend
    depends_on:
      - backend
    ports:
      - "5173:5173"